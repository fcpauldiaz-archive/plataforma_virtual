{% extends '::base_page.html.twig' %}

{% block body_content -%}
	<div class="blk">
	    <h1>Tutoria vote</h1>

	    {{ form(edit_form) }}

	        <ul class="record_actions">
			    <li>
			        <a href="{{ path('tutoria_all') }}">
			            Back to the list
			        </a>
			    </li>
			</ul>
	</div>
{% endblock %}
