{% extends '::base_page.html.twig' %}

{% block body_content -%}
	<div class="blk">
	    <h1>Tutoria edit</h1>

	    {{ form(edit_form) }}

	        <ul class="record_actions">
			    <li>
			        <a href="{{ path('tutoria') }}">
			            Back to the list
			        </a>
			    </li>
			    <li>{{ form(delete_form) }}</li>
			</ul>
	</div>
{% endblock %}
