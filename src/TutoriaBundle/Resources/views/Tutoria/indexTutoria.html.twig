{% extends '::base_page.html.twig' %}

{% block body_content -%}
    <h1>Mis Tutorías</h1>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Tutor</th>
                <th>Curso</th>
                <th>Informacion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tutoria in entities %}
        
        <tr>
            <td><a href="{{ path('tutoria_show', { 'id': tutoria.id }) }}">{{ tutoria.id }}</a></td>
            <td>{{ tutoria.usuario.getEmail()}}</td>
            <td>{{ tutoria.curso }}</td>
            <td>{{ tutoria.info }}</td>
                            

            <td>
            <ul>
                <li>
                    <a href="{{ path('tutoria_show', { 'id': tutoria.id }) }}">show</a>
                </li>
                <li>
                    <a href="{{ path('tutoria_edit', { 'id': tutoria.id }) }}">edit</a>
                </li>
            </ul>
            </td>
        </tr>
        
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tutoria_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
