{% extends '::base_page.html.twig' %}

{% block body_content -%}
    <h1>Documento</h1>

    <table class="table table-striped table-hover blk">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre Documento</th>
                <td>{{ entity.documentName }}</td>
            </tr>
            <tr>
                <th>Actualizado</th>
                <td>{{ entity.updatedAt|date('d-m-Y H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('documento') }}">
            Regresar
        </a>
    </li>
    <li>
        <a href="{{ path('documento_edit', { 'slug': entity.slug ,'username':app.user.username}) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
