{% extends '::base_page.html.twig' %}

{% block body_content -%}
    <h1>Parciales</h1>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre Documento</th>
                <th>Actualizado</th>
                <th>Link</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for document in entities %}
        {% if document.tipoDocumento == true %}
            <tr>
                <td><a href="{{ path('documento_show', { 'slug': document.slug }) }}">{{ document.id }}</a></td>
                <td>{{ document.documentName }}</td>
                <td>{% if document.updatedAt %}
                        {{ document.updatedAt|date('Y-m-d H:i:s') }}
                    {% endif %}
                </td>
                <td>
                    <a href="{{ vich_uploader_asset(document, 'documentFile') }}">Descargar</a>
                </td>

                <td>
                <ul>
                    <li>
                        <a href="{{ path('documento_show', { 'slug': document.slug }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('documento_edit', { 'slug': document.slug }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endif %}
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('documento_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
{% endblock %}

