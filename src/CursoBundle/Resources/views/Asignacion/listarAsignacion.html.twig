{% extends 'base_page.html.twig' %}

{% block body_content %}
    <h1>Cursos Asignados</h1>
	<table class = "table table-striped table-hover">
		<thead>
			<tr>
				<th>Curso</th>
				<th>Acción</th>
			</tr>
		</thead>
		<tbody>
			{% for curso in cursosAsignados %}
			<tr>
			
				<td> {{ curso.nombreCurso }} </td>
				<td> 
					<a class= "btn btn-default btn-xs btn-danger" href="{{ path('remove_curso', { 'curso_id':curso.id, 'username': app.user.username}) }}">
						Quitar Curso
					</a>
				</td>
				
			</tr>	
			{% endfor %}
		</tbody>
	</table>

	<a class= "btn btn-default btn-sm btn-warning" href="{{ path('asignacion', {'username': app.user.username}) }}">
		<h4>Asignar nuevo Curso</h4>
	</a>

{% endblock %}
